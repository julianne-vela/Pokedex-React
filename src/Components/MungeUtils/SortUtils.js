    // sortAsc = () => {
    //     const { pokeData, sortSelected } = this.state;
    //     this.setState({
    //         pokeData: [...pokeData].sort((a, b) => {
    //             if (a[sortSelected] > b[sortSelected]) {
    //                 return 1;
    //             }

    //             if (a[sortSelected] < b[sortSelected]) {
    //                 return -1;
    //             }

    //             return 0;
    //         })
    //     });
    // };

    // sortDesc = () => {
    //     const { pokeData, sortSelected } = this.state;
    //     this.setState({
    //         pokeData: [...pokeData].sort((a, b) => {
    //             if (a[sortSelected] > b[sortSelected]) {
    //                 return -1;
    //             }

    //             if (a[sortSelected] < b[sortSelected]) {
    //                 return 1;
    //             }

    //             return 0;
    //         })
    //     });
    // };